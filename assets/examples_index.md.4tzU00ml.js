import{_ as t,o,c as a,an as s}from"./chunks/framework.B2M8tloE.js";const u=JSON.parse('{"title":"Examples","description":"","frontmatter":{},"headers":[],"relativePath":"examples/index.md","filePath":"examples/index.md","lastUpdated":null}'),n={name:"examples/index.md"};function r(i,e,d,l,c,p){return o(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="Examples" tabindex="-1">Examples <a class="header-anchor" href="#Examples" aria-label="Permalink to &quot;Examples {#Examples}&quot;">​</a></h1><p>Every example here starts from one ordinary <strong>Julia</strong> file carrying <code>#~</code> metaLines in its comments. The smallest examples pair each input with the one rendered share-view it derives; the richest derive several outputs from a single source — each with its own view of what is shown, what stays hidden, and what runs. Two kinds of control are on display: <strong>control of rendering</strong> — which content each derived view carries — and <strong>control of execution</strong> — which code a notebook or a docs build actually runs.</p><p>The smallest examples are the repository&#39;s committed corpus: <strong>seven input/output pairs</strong> under <code>examples/</code> that render <strong>byte-identically</strong>, pinned by the shipped 1140-test suite at this release. Verify every pair yourself from a repository checkout:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-high-contrast github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>julia --startup-file=no --project=. -e &#39;using Pkg; Pkg.instantiate()&#39;   # once</span></span>
<span class="line"><span>julia --startup-file=no --project=. run_examples.jl</span></span></code></pre></div><h2 id="The-notebooks-pipeline" tabindex="-1">The notebooks pipeline <a class="header-anchor" href="#The-notebooks-pipeline" aria-label="Permalink to &quot;The notebooks pipeline {#The-notebooks-pipeline}&quot;">​</a></h2><p>One source, many outputs. <code>notebooks_from_source.jl</code>, at the repository root, derives a whole notebook family from ONE marked Julia source file — purely from its <code>#~</code> marks. From <code>notebooks/src/montecarlo.jl</code> — an ordinary, runnable Julia file (<code>include</code> it) — it emits <strong>four notebook editions</strong>; a standard <code>jupyter nbconvert --execute</code> run then produces the <strong>executed twin</strong>:</p><ul><li><p><strong>full</strong> — every surviving cell, with legend-driven tags (<code>parameters</code>, <code>skip-execution</code>, <code>solution</code>, <code>deep-dive</code>);</p></li><li><p><strong>student</strong> — the worksheet: the solution cell is absent, a scaffold cell stands in, and instruction cells are locked;</p></li><li><p><strong>report</strong> — the reading view: every code input folded;</p></li><li><p><strong>slides</strong> — a slide type per cell, scraped from the source&#39;s depth marks; <code>jupyter nbconvert --to slides</code> turns it into a reveal.js deck;</p></li><li><p><strong>executed</strong> — the one edition with real outputs, from a standard <code>jupyter nbconvert --execute</code> run.</p></li></ul><p>Each line&#39;s fate comes from the engine&#39;s own evaluated verdicts — the <code>:visib</code> rows of <code>GoMeta.altValues_evals</code> — never re-derived from the render. A verdict-hidden line is never visible in a rendered view, and never lost: it travels with the notebook in source form (an HTML comment in markdown cells, <code>gometa</code> cell metadata in code cells), so the derivation is a format transformation, not a projection. A <strong>discarded</strong> line travels nowhere. Hidden lines do not execute in notebooks.</p><p>A second source ships the same way: <code>notebooks/src/quickstart.jl</code> runs as a script from the checkout root <em>and</em> as its derived notebook — the onboarding artifact demonstrates the pipeline it teaches.</p><p>Every generated artifact regenerates <strong>byte-identically</strong> (the executed twin, which carries real kernel outputs, is validated separately, outside that byte gate):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-high-contrast github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>julia --startup-file=no --project=. notebooks_from_source.jl --check</span></span></code></pre></div><h2 id="Documenter-pages-as-views" tabindex="-1">Documenter pages as views <a class="header-anchor" href="#Documenter-pages-as-views" aria-label="Permalink to &quot;Documenter pages as views {#Documenter-pages-as-views}&quot;">​</a></h2><p>The same marks in the same source also derive a pair of Documenter manual pages — <code>docs/montecarlo-full.md</code> and <code>docs/montecarlo-reader.md</code>. The pages are <strong>views</strong>: verdict-hidden lines are simply not on them (the one exception is Documenter&#39;s own executed blocks — <code># hide</code>-marked <code>@example</code> lines and <code>@setup</code> bodies — which the docs build runs and display-hides while they remain present in the raw page markdown), and metaLines do not ride them. The <code>#~</code> marks additionally decide which <code>jldoctest</code> blocks each page carries — <strong>the docs build runs exactly the tests the marks kept</strong>. Rendering control and execution control come from the same metadata.</p><h2 id="Jupyter-notebook-example" tabindex="-1">Jupyter notebook example <a class="header-anchor" href="#Jupyter-notebook-example" aria-label="Permalink to &quot;Jupyter notebook example {#Jupyter-notebook-example}&quot;">​</a></h2><div class="info custom-block"><p class="custom-block-title">Coming at launch</p><p>A full worked Jupyter example lands here at launch — it is being finalized now. In the meantime, the complete notebook family ships in the repository under <code>notebooks/</code>, with a README that explains every artifact and how to regenerate it.</p></div><h2 id="Documenter.jl-example" tabindex="-1">Documenter.jl example <a class="header-anchor" href="#Documenter.jl-example" aria-label="Permalink to &quot;Documenter.jl example {#Documenter.jl-example}&quot;">​</a></h2><div class="info custom-block"><p class="custom-block-title">Coming at launch</p><p>A full worked Documenter.jl example lands here at launch — it is being finalized now. In the meantime, the derived manual-page pair ships in the repository under <code>docs/</code>, generated from <code>notebooks/src/montecarlo.jl</code> by the shipped generator at the repository root.</p></div><p>New to the syntax? Start with <a href="./../concepts/what-is-gometa">What is GoMeta?</a> and <a href="./../concepts/metalines">metaLines</a>, or install via <a href="./../tutorials/getting-started">Getting started</a>.</p>`,18)])])}const m=t(n,[["render",r]]);export{u as __pageData,m as default};
